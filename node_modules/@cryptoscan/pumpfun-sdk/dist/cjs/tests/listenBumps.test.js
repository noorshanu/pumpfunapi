"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const PumpApi_js_1 = require("../PumpApi.js");
const TIMEOUT = 5 * 1000;
(0, vitest_1.describe)('Listen bumps', () => {
    (0, vitest_1.test)('Should listen bumps', async () => {
        let idx = 0;
        const api = new PumpApi_js_1.PumpApi();
        api.onBump((coin) => {
            if ('signature' in coin) {
                console.log(coin.mint);
                idx += 1;
            }
        });
        await new Promise((resolve) => setTimeout(resolve, TIMEOUT));
        (0, vitest_1.expect)(idx > 5);
    }, TIMEOUT);
});
