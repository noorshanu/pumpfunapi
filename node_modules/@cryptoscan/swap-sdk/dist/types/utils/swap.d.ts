import { Connection, Keypair } from "@solana/web3.js";
import { type CreateTransactionParams } from "./createTransaction.js";
interface SwapBaseParams extends Omit<CreateTransactionParams, 'walletAddress'> {
    wallet: unknown;
}
type SolanaSwapParams<P = SwapBaseParams> = P & {
    network: 'solana';
    connection?: Connection;
    wallet: Keypair;
};
export type SwapParams<Params = SwapBaseParams> = SolanaSwapParams<Params>;
export declare const swap: ({ wallet, connection: paramsConnection, ...params }: SwapParams<SwapBaseParams>) => Promise<string>;
export {};
