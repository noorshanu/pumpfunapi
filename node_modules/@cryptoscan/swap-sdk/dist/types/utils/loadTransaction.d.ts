import { Transaction, VersionedTransaction } from "@solana/web3.js";
import { NetworkName } from "../types/NetworkName.js";
interface BaseParams {
    network: NetworkName;
    txn: string;
}
interface SolanaParams<IsLegacy> extends BaseParams {
    isLegacy?: IsLegacy;
}
export type LoadTransactionParams<IsLegacy> = SolanaParams<IsLegacy>;
export declare const loadTransaction: <IsLegacy, Response = IsLegacy extends true ? Transaction : VersionedTransaction>({ network, txn, isLegacy }: LoadTransactionParams<IsLegacy>) => Response;
export {};
