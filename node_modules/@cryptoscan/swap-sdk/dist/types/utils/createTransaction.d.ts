import { VersionedTransaction } from "@solana/web3.js";
import { NetworkName } from "../types/NetworkName.js";
interface BaseParams {
    type?: string;
    network: NetworkName;
    from: string;
    to: string;
    amount: number;
    fee?: number;
}
export interface CreateSwapParams extends BaseParams {
    type?: 'swap';
    service?: string;
    walletAddress: string;
    slippage?: number;
}
export interface CreateTransferParams extends BaseParams {
    type: 'transfer';
    coinAddress: string;
}
export type CreateTransactionParams = CreateTransferParams | CreateSwapParams;
export declare const createTransaction: (params: CreateTransactionParams) => Promise<VersionedTransaction>;
export {};
