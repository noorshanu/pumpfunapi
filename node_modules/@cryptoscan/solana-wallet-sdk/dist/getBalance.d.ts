import { Connection, type Commitment } from "@solana/web3.js";
export declare const getBalance: (walletAddress: string, coinAddress?: string, connection?: Connection, commitment?: Commitment) => Promise<number>;
