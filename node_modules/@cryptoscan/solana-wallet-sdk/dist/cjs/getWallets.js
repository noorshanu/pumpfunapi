"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getWallets = void 0;
const fs_1 = require("fs");
const getWallet_js_1 = require("./getWallet.js");
const getWallets = (path) => {
    const keysStr = (0, fs_1.readFileSync)(path).toString();
    const keys = keysStr.split('\n').filter((k) => !!k);
    const wallets = keys.map((k) => (0, getWallet_js_1.getWallet)(k));
    if (!wallets?.length) {
        throw new Error(`Missing wallets in the ${path}`);
    }
    return wallets;
};
exports.getWallets = getWallets;
